////////////////////////////
// Global key assignments //
////////////////////////////

// Movement
bind "w" "+forward"
bind "a" "+moveleft"
bind "s" "+back"
bind "d" "+moveright"

// Vertical movement
bind "space" "+all_jump"
bind "shift" "+all_duck"

// Special keys
bind "ctrl" "+special_move"
bind "q"    "+quick_weapon"
bind "f"    "+team_comm1"
bind "r"    "+team_comm2"

// Mouse: Attack 1+2+3, Melee, Voice
bind "mouse1" "+all_attack"
bind "mouse2" "+all_attack2"
bind "mouse3" "+all_attack3"
bind "mouse4" "+all_melee"
bind "mouse5" "+all_voice"
bind "mwheelup"   "+all_primary"
bind "mwheeldown" "+all_secondary"

// Low frequncy buttons
bind "l" "dropitem"
bind "m" "open_charinfo_direct"
bind "n" "open_charinfo_backpack"
bind "y" "voice_menu_1"
bind "x" "voice_menu_2"
bind "c" "voice_menu_3"
bind "t" "impulse 201" //Spray
bind "z" "say"
bind "u" "say_team"
bind "g" "+taunt"
bind "," "changeclass"
bind "." "changeteam"
bind "TAB" "+showscores"
bind "ESCAPE" "cancelselect"
bind "v" "callvote"
bind "enter" "player_ready_toggle"
bind "b" "+quickswitch"
bind "p" "askconnect_accept"

// Screenshot, Replay and Console
bind "F5" "screenshot"
bind "F6" "save_replay"
bind "F7" "abuse_report_queue"
bind "F12" "toggleconsole"

// For old-school votes (please die already!)
bind "1" "slot1"
bind "2" "slot2"
bind "3" "slot3"
bind "4" "slot4"
bind "5" "slot5"
bind "6" "slot6"
bind "7" "slot7"
bind "8" "slot8"
bind "9" "slot9"
bind "0" "slot10"

// Team Communication
bind "alt" "voicemenu 1 1" // Spy!
bind "e" "voicemenu 0 0"   // Medic!

// Taunt with different weapon. Only works on few servers.
bind "KP_END"       "taunt; slot1"
bind "KP_DOWNARROW" "taunt; slot2"
bind "KP_PGDN"      "taunt; slot3"

// Stuck in some prop? About to rage-quit?
bind "KP_5"			"kill"
bind "KP_UPARROW"	"explode"

// For the item test map. Just ignore.
bind "PGUP" "testitem"
bind "PGDN" "testitem_botcontrols"

// Steam Notifications. Give them some faster reachable buttons.
bind "KP_PLUS" "cl_trigger_first_notification"
bind "KP_MINUS" "cl_decline_first_notification"

// Press F1 if the HUD has the wrong team color after scramble:
bind "F1" "hud_reloadscheme"

// Press F2 if you are stuck in a broken spectate mode after selecting a team color:
bind "F2" "spectate"